# Проект: Учёт сырья и производства для мыловарни/косметической мастерской

## Статус
Стек пивотнулся со Streamlit+Google Sheets (исходное согласование с Claude claude.ai) на
FastAPI+PostgreSQL+React — см. раздел "Технический стек" ниже. Роадмап-шаги 1-8 реализованы
(логин, Сырьё/Компоненты, Производство, Рецепты, Отгрузка/Продукты, Сотрудники, Дашборд, Техпанель)
— см. "Роадмап реализации".
Этот файл — источник правды о проекте для Claude Code. При изменении решений — обновляй файл.

## Кто мы
- Основатель (Founder): Любовь Юрьевна Лаврухина
- Разработчик: пользователь этого проекта (junior, Python, PyCharm)

## Что за продукт
Внутренняя система для небольшой мастерской (мыло, свечи, косметика ручной работы):
- инвентаризация сыпучего и жидкого сырья (раз в месяц, возможно чаще)
- учёт расходов и пополнений сырья
- учёт производства партий по рецептам со списанием сырья
- учёт продаж готовых изделий
- КПД сотрудников по сменам
- дашборд с аналитикой
- Telegram-бот для быстрых запросов (низкий приоритет, см. "Роадмап реализации" п.9)

Требование к простоте интерфейса: должно быть управляемо человеком без технического бэкграунда ("любая обезьяна должна справиться"). Рядовой сотрудник не видит и не знает о существовании базы данных/API — только страницы React-приложения.

## Технический стек
- **Backend:** FastAPI (`backend/app/`), роутер на раздел (`app/routers/{auth,ingredients,production,packaging,recipes,products,sales,counterparties,dashboard}.py`)
- **База данных:** PostgreSQL через SQLAlchemy 2.0 (`app/models.py`), миграции — Alembic (`backend/alembic/`), DSN — `postgresql+psycopg://` в `app/config.py`/`DATABASE_URL`
- **Frontend:** React 19 + TypeScript + Vite (`frontend/src/`), Tailwind для стилей; страницы в `pages/`, модалки/детейл-панели в `components/`, `lib/api.ts` (`apiFetch`) и `lib/auth.tsx` (`useAuth`) — общий слой запросов и сессии
- **Пароли:** хэшируются через `bcrypt`, plaintext нигде не хранится (`app/security.py`)
- **Авторизация:** JWT (`pyjwt`) — сервер без состояния, фронт хранит токен и шлёт его в `Authorization` header; роль проверяется на каждом роутере через `require_roles(...)` (`app/security.py`), а не только скрытием пункта меню
- **Роли/константы:** `app/constants.py` (`FOUNDER`, `DEVELOPER`, типы транзакций, `PRODUCT_REQUIRED_FIELDS`)
- **Бот:** Telegram-бот (шаг 9 роадмапа, ещё не реализован) — при реализации будет ходить в тот же Postgres/FastAPI, а не напрямую в БД

### Почему не Google Sheets
Исходно планировался Streamlit+gspread (Google Sheets как база) — простота и отсутствие инфраструктуры перевешивали. От этого отказались в пользу Postgres+FastAPI+React ради нормальной типизации данных, ролевой модели через JWT и SPA-интерфейса вместо Streamlit rerun-модели. Раздел "Структура данных" ниже описывает те же сущности, что раньше были листами Google Sheets — теперь это таблицы Postgres (см. `app/models.py`), но набор полей и связи между ними не менялись.

## Структура данных

Ниже — те же сущности, что раньше были листами двух Google Sheets-файлов (см. "Почему не Google
Sheets"). Теперь это таблицы одного Postgres, определённые в `backend/app/models.py`; разделения на
"файл с рабочими данными" и "закрытый файл с доступами" на уровне БД больше нет — это было решение
специфичное для ACL Google Sheets (права выдаются на файл целиком). Разграничение по ролям теперь
на уровне API (`require_roles`), не на уровне БД/файла.

**Company** — тенант (мультитенантность, добавлена 2026-07-16, см. отдельный раздел ниже)
`id, название, дата создания`
Почти все остальные таблицы ниже несут `company_id` — исключение только RecipeItem (скоуп
транзитивно через recipe_id/material_id).

**Materials** — справочник сырья
`id, название, категория, ед.измерения, мин.остаток, текущий остаток, архив`
Категория — свободная строка в БД (`String(20)`, без CHECK/enum). Базовая тройка кодов —
`тара`, `косм` (косметика), `свеч` (свечи) — так пришли из импорта «oinarri рецептуры», не
`сыпучее`/`жидкое`, как было в исходном плане (0 материалов с такими значениями на живых данных,
2026-07-21). `GET /api/ingredients/categories` отдаёт эту тройку + любые кастомные категории,
уже использующиеся в компании; `NewIngredientModal.tsx` даёт создать новую категорию прямо в форме
("Добавить категорию" после пунктирной черты в выпадающем списке) — свободный ввод, не ограничено
тройкой. Полное название на экране — через `materialCategoryLabel()` (`lib/labels.ts`), само
значение в БД не трогается. Категория «тара» — упаковка (флаконы, крышки, этикетки), списывается по
рецепту наравне с сырьём.
Архив (2026-07-21, тот же паттерн, что у Recipe) — архивный материал не попадает в список по
умолчанию (`GET /api/ingredients?archived=false`, дефолт) и не всплывает в "остатки ниже минимума"
на дашборде, но остаток/история движений/связи из RecipeItem не трогаются — фильтруется только
видимость. Переключатель Founder/Developer, см. `IngredientDetailPanel.tsx`.
Подкатегория тары (2026-07-23, запрос Александра) — `packaging_type` (короб/флакон/наклейка/
лента/прочее, `PACKAGING_TYPES` в `constants.py`) + типовые поля `width_mm`/`height_mm`/
`length_mm`/`volume_ml`/`material_finish`/`tape_feature`, осмысленны только при
`category="тара"`, справочные — в себестоимость не входят (та по-прежнему только через
`unit_cost`/лоты, см. "Себестоимость по лотам"). Раздел «Упаковка» в сайдбаре (`/packaging`,
`PackagingMaterialsPage.tsx`) — теперь каталог тары с вкладками по `packaging_type`, а не журнал
упаковки продукта (см. ProductionLog ниже, тот функционал туда и перенесён). Создание —
`NewPackagingMaterialModal.tsx`, поля меняются формой в зависимости от выбранного типа.

**Transactions** — единый журнал движений сырья (сердце системы)
`id, дата, material_id, тип (приход/расход/корректировка), кол-во, цена, freight_cost, recipe_id (если списание по производству), комментарий`
Остаток = сумма приходов − расходов ± корректировки, считается на лету SQL-запросом (не хранится статично).
`POST /api/production` перед списанием проверяет остаток каждого материала рецепта — если сырья не
хватает на запрошенное кол-во партий, возвращает 400 и не пишет ни одной транзакции (см. `production.py`).
`freight_cost` (2026-07-21) — доля транспортных расходов поставки, отнесённая на эту строку прихода;
`price` остаётся закупочной ценой поставщика без искажений. Заполняется только через групповой приход
(`POST /api/ingredients/income/batch`, см. "Себестоимость по лотам" ниже) — обычный одиночный приход
этого поля не трогает.

**Себестоимость по лотам** (2026-07-21, запрос Александра, `app/costing.py`) — принцип "дешёвый лот
первым": каждый приход — лот с ценой (закупка + доля `freight_cost`), расход съедает лоты по
возрастанию цены. Не отдельная таблица — считается на лету по истории Transaction того же материала,
как остаток. Корректировки в лоты не лезут (осознанное упрощение — не путают себестоимость, только
двигают остаток). `POST /api/ingredients/income/batch` — групповой приход нескольких материалов одной
поставкой + одна сумма транспортных расходов, делится между материалами пропорционально весу
(`min_purchase_batch_weight`/`min_purchase_batch_qty` с карточки материала; без этих полей — fallback
на пропорцию по количеству). Продукт → себестоимость партии/единицы = сумма по составу рецепта
(`RecipeItem.qty_per_batch` × себестоимость активного лота материала), показывается на карточке
продукта (`GET /api/products`, поля "себестоимость партии"/"себестоимость единицы"); `None`, если хотя
бы по одному материалу рецепта нет ни лота, ни ручной "себестоимости 1 шт" — не показываем частично
посчитанную цифру.

**Recipes** — рецепты
`id, название, категория, что производим, выход партии, технология, архив, процент потерь`
Поле «технология» добавлено при переносе реальных рецептов — пошаговая инструкция приготовления, у founder в исходнике была для каждого рецепта. Поле «категория» — выбор из уже существующих категорий Product (не свободный ввод, не отдельный справочник), состав рецепта обязателен при создании.
«Процент потерь» (2026-07-21, запрос Александра) — сверх точного расчёта по составу рецепта,
накидывается при списании сырья в `POST /api/production` (`loss_factor = 1 + loss_percent/100`,
умножает и проверку достаточности остатка, и саму сумму списания) и в расчёте себестоимости
продукта (`app/costing.py::compute_product_costs`) — иначе оба числа были бы занижены относительно
того, что реально уходит со склада. Дефолт 3% на уровне модели (`Recipe.loss_percent`,
`server_default='3'`) — глобальный старт, редактируется per-рецепт через `PATCH /api/recipes/{id}`
(`RecipeDetailPanel.tsx`, инлайн-редактирование, Founder/Developer). `PATCH /api/recipes/{id}`
теперь общий partial-update (`UpdateRecipeRequest`, `exclude_unset`), не только для archived —
переиспользуется тем же эндпоинтом, что и переключатель архива.

**RecipeItems** — состав рецепта
`recipe_id, material_id, кол-во на 1 партию`
В Sheets-эпоху здесь дублировались "название рецепта"/"название материала" (Founder читала лист
глазами). В Postgres это лишний денормализованный столбец, который может разъехаться при
переименовании — таблица хранит только id, названия при чтении джойнятся из Recipes/Materials.

**Products** — готовые изделия (мыло, свечи и т.д.), карточка товара
`id, название, категория, GTIN, состав, recipe_id, ТН ВЭД, декларация соответствия, срок действия РД`
Обязательные при создании: название, категория, GTIN (`app.constants.PRODUCT_REQUIRED_FIELDS`).
Остальное опционально. "Название рецепта" в БД не хранится — джойнится через `recipe_id` при чтении
(см. `app/routers/products.py`). "Себестоимость партии"/"себестоимость единицы" — не хранятся,
считаются на лету при каждом чтении (см. "Себестоимость по лотам" выше).

**Counterparties** — контрагенты-покупатели (юрлица)
`id, название, ИНН, КПП, ОГРН, юр.адрес, телефон, контактное лицо, комментарий`
Заполняются Founder/Developer в разделе «Контрагенты» и привязываются опционально к Sale при отгрузке.
Автоподстановка по ИНН (2026-07-19) — `GET /api/counterparties/lookup?inn=` ходит в
suggestions.dadata.ru (`app/config.py::DADATA_API_KEY`, пусто по умолчанию → 501, фронт
тихо остаётся в обычном ручном режиме, ничего не ломает). На успехе фронт
(`NewCounterpartyModal.tsx`) блокирует найденные поля (название/КПП/ОГРН/адрес) от
ручного редактирования — просьба Founder исключить случайные опечатки при заведении
контрагента; «Ввести вручную» снимает блокировку, если DaData ошиблась/устарела.

**Sales** — продажи готовых изделий
`id, дата, product_id, counterparty_id (опционально), кол-во, цена, комментарий`
`POST /api/sales` проверяет «готово к отгрузке» продукта (см. `products.py::_packaged_by_product`) —
если продано больше, чем упаковано минус уже проданное, возвращает 400. Продукты без `recipe_id`
(не привязаны к рецепту) проверке не подлежат — для них «готово к отгрузке» не считается.
Полный пайплайн (2026-07-23, решение Александра): рецепт → производство (списание сырья) →
упаковка → «готово к отгрузке» → отгрузка. До этого база была ProductionLog (сырое изготовление,
без учёта упаковки) — переключено на PackagingLog, т.к. Production и Packaging слились в одну форму
в тот же день (см. ProductionLog ниже) и товар физически готов к отгрузке только после упаковки.
Блокировка от гонки при одновременных продажах (2026-07-21, code-review) раньше лочила строку Recipe
как прокси-мьютекс — теперь лочит сам `Product` (осмысленнее: готовность больше не завязана на
рецепт, только на product_id через PackagingLog).

**ProductionLog** — журнал смен производства
`id, дата, worker_id, recipe_id, кол-во партий, время начала/окончания, кол-во брака, комментарий`
Источник данных для КПД. КПД = количество партий + скорость (время на партию) + брак/ошибки.
`GET /api/production/leaderboard` — отдельный «лидерборд»-эндпоинт (не dashboard/kpi): кол-во
произведённого (партии×выход_партии − брак) за сегодня и с начала месяца по каждому сотруднику,
доступен ВСЕМ ролям включая worker (осознанное решение Founder — мотивация, все видят, кто сколько
делает). Отдаёт только количество — цен/выручки в ProductionLog нет вообще, изоляция не на фронте,
а в том, что этих полей физически нет в ответе. Не путать с `dashboard/kpi` (founder/developer,
полная история помесячно) — тот остаётся закрытым, там нет такого ограничения по данным.
Изготовление + упаковка одной формой (2026-07-23, запрос Александра) — `POST /api/production`
принимает опциональные `packaged_qty`/`packaged_defects` вместе с обязательным `product_id`
(раньше был не обязателен — recipe_id выводился из выбранного продукта на фронте, но сам id
продукта не передавался); если `packaged_qty > 0`, тем же запросом пишется отдельная строка в
PackagingLog (см. ниже, модель не изменилась). Раньше это было отдельной вкладкой/журналом —
теперь `NewProductionModal.tsx` покрывает оба шага одной формой ("Упаковка (необязательно)" —
доп. поля внизу формы производства).

**PackagingLog** — журнал упаковки (модель), больше не отдельный раздел меню
`id, дата, worker_id, product_id, кол-во, брак, комментарий`
Фиксирует момент упаковки готового изделия после производства — отдельный шаг от ProductionLog
(производство сырой партии) и Sale (отгрузка). С 2026-07-23 это ИСТОЧНИК расчёта «готово к
отгрузке» у Product (см. `app/routers/products.py::_packaged_by_product`) — раньше считалось от
ProductionLog, теперь от PackagingLog (см. Sales выше, там же история решения). `POST /api/packaging` (роутер
`packaging.py`) оставлен как есть (не удалён, backward-compat/API-полнота), но с 2026-07-23 не
используется UI напрямую — запись через него теперь создаёт `POST /api/production` (см. выше).
Раздел «Упаковка» в сайдбаре (`/packaging`) для этого журнала как отдельной вкладки БОЛЬШЕ НЕТ —
пункт меню переиспользован под каталог тары (см. Materials выше), история упаковки читается через
`GET /api/packaging`, встроена в `ProductionPage.tsx` вторым блоком ("История упаковки") под
основным журналом производства.

**Feedback** — обратная связь для разработчика
`id, дата, автор, роль автора, сообщение, статус (новое/просмотрено/решено)`
Доступна для отправки всем ролям. Список и статусы видят только Founder и Developer.

**Users**
`id, ФИО, логин, хэш пароля (bcrypt), роль (founder/worker/developer), статус (активен/уволен), дата создания, телефон, мессенджер, адрес проживания, документ (текстовое поле, например паспорт/ИНН)`
Учётные записи создаёт только Founder (или Developer) через интерфейс приложения. Раньше это был
физически отдельный Google Sheets-файл (права выдаются на файл целиком) — в Postgres это обычная
таблица в той же БД, изоляция обеспечивается на уровне API (`require_roles`), не на уровне БД.
Телефон валидируется на бэке (`app/schemas.py`) — только цифры и `+ - ( )`, буквы отклоняются.

**LoginLog** — журнал входов (кто и когда авторизовался)
`id, user_id, дата и время`
Пишется на каждый успешный `POST /api/auth/login`. Список (`GET /api/auth/log`) виден только
Founder/Developer — выводится в разделе «Сотрудники».

## Роли и права

| Действие | Founder | Worker | Developer |
|---|---|---|---|
| Внести производство/упаковку/остатки | ✅ | ✅ | ✅ |
| Свой КПД | ✅ | ✅ | ✅ |
| Лидерборд — кол-во произведённого сегодня/за месяц, все сотрудники | ✅ | ✅ | ✅ |
| КПД всех сотрудников (полная история, dashboard/kpi), продажи, финансы, дашборд | ✅ | ❌ | ✅ |
| Управление рецептами | ✅ | ❌ | ✅ |
| Добавить/уволить сотрудника | ✅ | ❌ | ✅ |
| Техническая панель (логи, статус API, сброс кэша) | ❌ | ❌ | ✅ |
| Заводить новые компании (тенанты) | ❌ | ❌ | ✅ |

Worker НЕ видит чужой полный КПД (dashboard/kpi — история, брак, финансы) и вообще не знает о
существовании базы данных/API — работает исключительно со страницами React-приложения. Исключение —
production/leaderboard (см. ProductionLog выше): осознанно открытая всем ролям витрина только
количества произведённого за сегодня/месяц, отдельная от закрытого dashboard/kpi.

**Founder не может назначить роль Developer** (2026-07-21, code-review) — `POST /api/users` и
`PATCH /api/users/{id}` раньше проверяли только, что `role` вообще существует в `USER_ROLES`, не
кто её назначает: Founder мог создать себе Developer-аккаунт и получить Техпанель/Компании в обход
таблицы ролей выше. Теперь `role == DEVELOPER` в теле запроса требует, чтобы вызывающий сам был
Developer, иначе 403.

**Ограничение входа worker'ов по сети мастерской** (2026-07-23/24, запрос Александра, опционально
по компании) — настраивается на `StaffPage.tsx` (`GET/PUT /api/users/network-settings`,
founder/developer). Изначально (2026-07-23) было через DDNS-хостнейм, резолвящийся на каждый
вход — **переделано 2026-07-24 на call-home/heartbeat**, т.к. DDNS-провайдеры (DuckDNS и
подобные) часто недоступны из РФ. Механика: `POST /api/public/workshop-ping/{token}` — публичный
эндпоинт без авторизации (128-битный токен в пути сам по себе авторизация, `secrets.token_hex(16)`,
генерится лениво при первом включении в `PUT /api/users/network-settings`), роутер мастерской
стучится сюда раз в несколько минут (cron), сервер запоминает `Company.worker_network_ip` = IP
звонившего (виден автоматически из самого HTTP-запроса, `rate_limit.py::client_ip`, тот же
X-Forwarded-For-парсинг, что у rate-limit) + `worker_network_ip_updated_at`. `security.py::
enforce_workshop_network` на каждом входе (`/login`, `/select-company`, `/switch-company`) просто
сверяет IP запроса с последним записанным — без DNS вообще. `Company.worker_network_enabled` —
явный тумблер, отдельный от наличия IP (можно включить раньше первого пинга). Fail-closed: нет
IP, или последний пинг старше 15 минут (`_WORKSHOP_PING_STALE_MINUTES` в security.py — роутер
считается недоступным) — отклоняем вход, не пропускаем молча. Только для роли `worker`,
founder/developer не ограничены (может понадобиться доступ удалённо). На роутере ничего
устанавливать не нужно — `curl`/`uclient-fetch` есть в стоке OpenWrt, команда для cron с готовым
токеном показывается прямо на `StaffPage.tsx`.

## Архитектурные принципы, которые нельзя нарушать
1. Ни один прямой доступ/ссылка на БД не должен быть виден в интерфейсе Worker.
2. Проверка роли — на каждом роутере программно (`require_roles(...)` в `app/security.py` как FastAPI-зависимость), а не только скрытием пункта меню на фронте — иначе можно обойти через прямой запрос к API.
3. Пароли — только bcrypt-хэш, никогда plaintext.
4. JWT — секрет только из `JWT_SECRET` (env), никогда не хардкодить. `get_current_user` перепроверяет статус пользователя в БД на каждый запрос (уволенный теряет доступ сразу, не дожидаясь истечения токена) — роль, `company_id` и `company_name` при этом берутся из payload токена, не перечитываются из БД, так что смена роли/компании/названия компании применяется только после переиздания токена (следующий логин), это осознанный компромисс, не баг (пользователь и так не переезжает между компаниями на практике). `company_id`/`company_name` — оба обязательны, отсутствие любого из двух форсирует чистый 401 (не просто одного `company_id`) — иначе фронтовый фоллбек на вайт-лейбле мог показать чужой бренд компании, у которой токен не нёс company_name (см. "Мультитенантность").
   **2026-07-21, найдено на code-review и подтверждено вживую на проде:** `JWT_SECRET` не был
   выставлен в Render env — приложение молча работало на захардкоженном dev-фоллбеке
   (`app/config.py`), а значит подделать токен под любой ролью/компанией мог любой, кто читал
   этот файл. `app/config.py` теперь падает при старте (`SystemExit`), если `JWT_SECRET` не
   задан и `RENDER=true` в env (Render сам его проставляет на каждый сервис — надёжный признак
   "не локалка"); локальный dev-фоллбек остаётся только вне Render. Секрет добавлен в Render
   Environment после находки — все существовавшие на тот момент токены (включая подделанные
   старым фоллбеком) стали недействительны, каждому потребовался повторный вход.
   `_find_user`/`attach_or_create_membership` также раньше искали логин через `ilike()` —
   `%`/`_` в самом логине трактовались как SQL-wildcard, открывая перебор существующих логинов
   через паттерн-матчинг (и позволяя завести аккаунт с буквальными `%`/`_` в логине). Заменено
   на точное регистронезависимое сравнение (`func.lower(User.login) == ...`). Заодно закрыт
   time-based оракул в `authenticate()` — путь "логина не существует" теперь тоже прогоняет
   `bcrypt.checkpw` (против фиктивного хэша), а не завершается мгновенно до вызова bcrypt, чтобы
   время ответа не отличало существующий логин от несуществующего.
5. Мультитенантность: каждый роутер обязан фильтровать ВСЕ запросы по `user["company_id"]` и проставлять его при создании записей — утечка данных между компаниями через забытый фильтр является критическим багом, не обычным. RecipeItem — единственное исключение без своего company_id (скоуп транзитивно через recipe_id/material_id, оба проверяются на принадлежность компании при создании/изменении состава).

## Мультитенантность (добавлена 2026-07-16)

Проект перешёл от однотенантной мастерской к изоляции нескольких независимых компаний в одной
БД — Founder решил попробовать пустить сюда не только свою мыловарню, но и, например, 3Д-печатную
мастерскую (вариант А из обсуждения — реальная изоляция, не просто общность модели данных).

- **Company** — тенант. `company_id` есть почти на каждой таблице (см. "Структура данных").
- **Онбординг новой компании — два пути.** (1) `POST /api/companies` (`app/routers/companies.py`,
  2026-07-18) — Developer заводит компанию прямо из приложения (пункт «Компании» в меню профиля,
  только у Developer, Founder такого пункта не видит вообще). Создаёт компанию + сразу первого
  **Developer**-аккаунта этой компании — НЕ Founder, того заводит тот же разработчик отдельно после
  (через `create_founder.py --company-id`, п.2 ниже, или это станет отдельной кнопкой позже). Это
  единственный роутер, намеренно не фильтрующий по `user["company_id"]` — вся его задача cross-tenant
  (список всех компаний + создание новой), см. "Архитектурные принципы" п.5. (2) CLI-скрипт
  `backend/scripts/create_founder.py` остаётся для бутстрапа первой компании на чистой БД (там ещё
  некому зайти в интерфейс) и для добавления второго Founder к уже существующей компании:
  `python -m scripts.create_founder --company-name "Новая мастерская" --fio "..." --login ... --password ...`
  (или `--company-id <id>` вместо `--company-name`, не плодя дубликат компании).
  Оба пути — **не** самостоятельная регистрация: требуют существующего Developer-аккаунта с доступом
  к приложению/БД, не публичная форма (та отдельно и по-прежнему не в реализации, см. ниже).
- **Логин глобально уникален** (не per-company) — экран входа не спрашивает "какая компания", она
  определяется автоматически по аккаунту. `company_id` живёт в JWT-payload наравне с ролью (см.
  "Архитектурные принципы" п.4) — не перечитывается из БД на каждый запрос.
- **Раскладка дашборда** (`DashboardWidgetLayout`) — per-user с 2026-07-20 (была общая на всю
  систему до мультитенантности, затем per-company; см. п.7 роадмапа ниже) — уникальность
  `(company_id, user_id, widget_key)`.
- **Изоляция проверяется явно** — `backend/tests/test_multitenancy.py`, отдельный файл, не
  разбросано по остальным тестам. Кросс-компанийный доступ по чужому id всегда 404 (не 403) —
  не подтверждаем даже факт существования чужой записи.
- Первая компания (существующая мастерская, "oinarri") — результат бэкфилл-миграции
  `a772deab8f2a_multi_tenant_company_id.py`, фиксированный id `a0000001`.
- **Публичная self-serve регистрация** (2026-07-18, реализована) — `POST /api/auth/register`
  (`app/routers/auth.py`, без `require_roles`/`get_current_user` — второй такой публичный
  POST-эндпоинт в приложении, первый — `POST /api/public/workshop-ping/{token}`, см. "Роли и
  права" → "Ограничение входа worker'ов по сети мастерской", там токен сам себе авторизация),
  `RegisterPage.tsx` на `/register`. Создаёт компанию + первого **Founder**-аккаунта
  (не Developer — тот только для internal-онбординга, `POST /api/companies` выше) и сразу логинит
  (тот же `{access_token, user}`, что и `/login`). Переиспользует `attach_or_create_membership` —
  если введённый логин уже существует у другого человека, привязывает его Founder'ом новой компании
  (тот же "вариант А", тот же пароль-чек). Обязательные поля: название компании, ФИО, телефон, логин,
  пароль — телефон с прицелом на будущую SMS/Telegram-верификацию, сам механизм которой пока не выбран
  (см. ниже). Rate-limit 5/час по IP (`app/rate_limit.py`, in-memory, читает `X-Forwarded-For` — Render
  проксирует, `request.client.host` был бы IP прокси, не посетителя, security-review 2026-07-18 поймал
  до пуша). Капчи пока нет — осознанная задолженность (см. ниже). Публичный анонимный эндпоинт ловит
  400 от `attach_or_create_membership` и подменяет сообщение на общее — "неверный пароль" и "уволен"
  по отдельности превратили бы эндпоинт в оракул для перебора чужих логинов (тот же security-review).
  `RegisterCompanyRequest` (`schemas.py`) — `max_length` на company_name/fio/login (совпадает с длиной
  колонок Postgres — без этого анонимный запрос ронял бы 500 вместо чистого 422, SQLite в тестах эту
  ошибку не ловит, маскировала баг) и байтовая (не символьная) проверка пароля ≤72 байт — bcrypt≥4.2
  кидает `ValueError` на пароле длиннее, кириллица 2 байта/символ. Тесты — `test_register.py`, включая
  отдельный regression на изоляцию данных нового тенанта (не полагается только на общий механизм в
  `test_multitenancy.py`).
  **Задолженность (сознательно, не блокирует первый заход):** капча (hCaptcha/Turnstile — оба
  бесплатные), телефон собирается, но не верифицируется — механизм (SMS-код или через Telegram-бота,
  требует своего бота + Telegram-аккаунт у клиента) ещё не выбран Founder'ом.

## Мульти-компанийные пользователи (добавлена 2026-07-18)

Founder попросил: один и тот же человек (сначала — сам разработчик как Developer, но
потенциально и Founder/Worker) может обслуживать/работать сразу в нескольких компаниях,
без необходимости заводить отдельный логин на каждую.

- **CompanyMembership** (`app/models.py`) — связь user↔company↔role, `unique(user_id, company_id)`.
  `User` больше НЕ несёт `company_id`/`role` напрямую (были там до 2026-07-18, перенесены сюда
  миграцией `2963dcbe68c7_company_memberships.py`) — `User` теперь чистая личность (ФИО, логин,
  пароль, `status`, телефон/мессенджер/адрес/документ). **Все эти поля остаются глобальными на
  User** (уволен = уволен из всех компаний сразу; смена телефона/ФИО/документа в одной компании
  видна во всех, где состоит человек) — осознанное решение: это один реальный человек с одним
  именем/паспортом/телефоном в жизни, не разные личности по компаниям. Per-membership копии этих
  полей не запрашивались и не сделаны.
- **Вход с одной компанией** (99% пользователей) — не меняется ни на бит: `POST /api/auth/login`
  сразу отдаёт `access_token` + `user`, как и раньше.
- **Вход с несколькими компаниями** — `authenticate()` (`app/security.py`) не логинит сразу,
  отдаёт `{needs_company_choice: true, pending_token, companies: [...]}`. Фронт (`LoginPage.tsx`)
  показывает список компаний вместо навигации; `POST /api/auth/select-company` с `pending_token`
  + выбранным `company_id` финализирует вход. `pending_token` — отдельный короткоживущий (5 мин)
  JWT-тип (`PENDING_TOKEN_TYPE`), не полноценная сессия — годится только на этот один эндпоинт.
- **Переключение компании уже залогиненным** — `POST /api/auth/switch-company`, без повторного
  пароля (личность уже подтверждена), просто переиздаёт токен на другое членство того же
  человека. В сайдбаре (`Sidebar.tsx`) — блок «Другие компании» в меню профиля, виден только
  если у юзера больше одного членства.
- **JWT payload несёт `companies: [{id, name, role}]`** — список ВСЕХ компаний юзера, не только
  активной, посчитан на момент выдачи токена. Как и `role`/`company_name`, не перечитывается из
  БД на каждый запрос (см. п.4 архитектурных принципов) — смена состава компаний применяется
  только после переиздания токена (следующий логин/переключение), тот же осознанный компромисс.
  `get_current_user` фоллбечит на `[текущая компания]`, если поля нет (токены до 2026-07-18).
- **Существующий логин при создании — не ошибка, а приглашение** (вариант А, решение Founder),
  единый helper `attach_or_create_membership` (`app/security.py`) — используется в
  `POST /api/companies`, `POST /api/users` и `create_founder.py`, не три отдельные копии.
  Если введённый логин уже существует у другого человека:
  - **Требует правильный пароль существующего аккаунта.** Без этой проверки любой Founder/
    Developer мог бы привязать ЧУЖОЙ аккаунт к своей компании, зная только логин (не пароль),
    и через `reset-password` перехватить ГЛОБАЛЬНЫЙ пароль — это была реальная дыра, поймана
    security-review 2026-07-18 до пуша в прод, не гипотетическая. Пароль в форме поэтому НЕ
    игнорируется (в отличие от ФИО/телефона и т.п. — те действительно игнорируются, у
    человека уже есть свой аккаунт).
  - **Уволенный существующий аккаунт (status ≠ активен) — отклоняется**, не привязывается
    молча. Без этой проверки founder думал бы что "нанял" рабочего, а тот не смог бы зайти
    (status глобальный, увольнение из одной компании блокирует все).
  - Дубликат (тот же user_id уже состоит в ТОЙ ЖЕ компании) — по-прежнему ошибка 400.
  - Фронт (`NewStaffModal.tsx`/`NewCompanyModal.tsx`) показывает предупреждение, если бэк
    вернул `attached_existing: true` — иначе founder не узнал бы, что "новый сотрудник" на
    самом деле уже существующий человек, а не свежесозданный аккаунт.
- **Сброс пароля** (`POST /api/users/{id}/reset-password`) меняет пароль на `User`, то есть сразу
  во ВСЕХ компаниях, где состоит человек — это осознанное поведение (один пароль на личность),
  не баг.
- **`get_current_user` отклоняет `pending_token`** явно (проверка `payload.get("type")`) — без
  этой проверки pending-токен проходил `jwt.decode` как обычный и падал `KeyError`→500 вместо
  чистого 401 (был баг, пойман code-review 2026-07-18 до пуша).
- **`switch-company` тоже пишет `LoginLog`** (той компании, куда переключились) — иначе
  Founder/Developer этой компании не видел бы в истории входов доступ через переключалку,
  только через прямой логин.
- Тесты: `test_multi_company_login.py` (вход/select-company/switch-company, включая отказ
  чужого pending_token и обычного access_token вместо pending), `test_companies.py` и
  `test_users.py` (вариант А — привязка существующего логина, неверный пароль отклоняется,
  уволенный существующий аккаунт отклоняется).

## Отложенные темы (пока не в реализации)
- ~~Упаковка ↔ «готово к отгрузке»~~ — решено 2026-07-23, см. раздел Sales выше: «готово к
  отгрузке» теперь считается от PackagingLog, не от ProductionLog.
- **Интеграция с "Честным знаком"**: рассмотрена и отложена. Не все товары мастерской попадают под обязательную маркировку на данный момент. Если понадобится — это отдельный опциональный источник данных для Sales (например поле `chestny_znak_code`), не блокирует MVP.
- **Видеонаблюдение**: Phase 1 (только живой просмотр + ручной скриншот, без записи/перемотки)
  реализован 2026-07-23 — `CameraSettings`/`SurveillanceScreenshot` в `models.py`, роутер
  `surveillance.py` (`GET/PUT /api/surveillance/settings`, `GET/POST /api/surveillance/screenshots`,
  founder/developer, company-scoped), `SurveillancePage.tsx` — плеер сам определяет формат по
  расширению настроенного URL: `.m3u8` → HLS через `hls.js` (Safari — нативно, без библиотеки),
  иначе — прямой `<img>` для MJPEG/HTTP-потока с самой камеры. Скриншот — canvas.drawImage из
  видео/img в PNG (base64), сохраняется в Postgres (не файловая система Render — эфемерная).
  Требует `crossOrigin="anonymous"` на источнике потока и CORS-заголовки на стороне моста —
  без них `canvas.toDataURL()` бросает `SecurityError` (tainted canvas), страница ловит это и
  показывает понятную ошибку вместо краша. Ждёт **только сетевой мост** (см. блокер ниже) —
  когда появится рабочий URL потока, Founder/Developer вводит его в настройках раздела, код
  готов уже сейчас.
  **2026-07-17, уточнено на месте**: камера на деле **IP, не аналог** (запись про Hunter
  HN-B45IR/коаксиал была ошибочной — либо камера другая, либо перепутали при покупке; заднюю
  панель камеры физически не осматривали до 2026-07-17). Разъём сзади камеры — сразу RJ45,
  подключена в тот же роутер, что и остальная сеть мастерской. Обнаруженный рабочий доступ:
  IP `192.168.1.77` (DHCP, не статика/не резервация — может смениться при ребуте роутера, надо
  зафиксировать), логин `admin`, пароль **заводской по умолчанию** (нашёлся перебором стандартных
  вариантов для этой прошивки) — **сменить на нормальный, задача не выполнена**. RTSP подтверждён
  рабочим через VLC (`rtsp://admin:<пароль>@192.168.1.77:554/stream1`, логин/пароль см. в
  менеджере паролей, не здесь — файл коммитится в git). DVR/регистратора нет и не будет — не
  нужен, раз камера сама отдаёт RTSP.
  > ⚠️ Пароль камеры до 2026-07-17 короткое время лежал здесь открытым текстом (security-review
  > поймал перед коммитом) — если этот файл когда-либо был запушен с паролем, считать пароль
  > скомпрометированным и сменить его на камере при первой возможности, даже после того как
  > текст уберётся отсюда.

  Фичspec от Founder (2026-07-17): вкладка «Видеотрансляция» в сайдбаре (Founder/Developer) —
  живой лайв при переходе на вкладку, запись хранится 3 дня (rolling), в режиме просмотра —
  перемотка в пределах текущего дня, скорость воспроизведения x0.5/x2/x3/x10, полноэкранный
  режим, скриншот кадра, регулировка яркости/контрастности изображения. Позже возможно —
  детекция активности человека на рабочем месте (не в текущем скоупе).

  **Блокер архитектуры (не решён)**: камера в локальной сети мастерской за роутером, прод
  (Render+Neon) — облако, прямого доступа к локалке нет. Нужен посредник — либо (А) физический
  сервер/mini-PC в мастерской, круглосуточно пишет RTSP через ffmpeg (HLS для лайва, retention
  3 дня через cron), наружу торчит через Tailscale/Cloudflare Tunnel или проброс порта — прод
  проксирует/встраивает плеер; либо (Б) камера напрямую в интернет через port-forwarding —
  не рекомендуется (нет нормального firewall/патчей прошивки на бюджетной камере). Без этого
  решения бэкенд-стриминг не начат; фронт (плеер, контролы) можно делать параллельно.

  **Решение по железу (2026-07-17)**: один роутер на OpenWrt, а не отдельный сервер и не два
  устройства (два роутера явно исключены пользователем — только один, чуть подороже). Роутер —
  уже имеющийся у пользователя **Xiaomi AX3000T** (MediaTek Filogic 820/MT7981B, 256MB RAM) —
  потянет одновременно Tailscale (мост камера↔облако, subnet router на 192.168.1.0/24) и
  Podkop (DPI-обход блокировок для сотрудников — Instagram/YouTube), т.к. Podkop роутит через
  VPN только заблокированные домены, не весь трафик. Перед прошивкой — сверить точную ревизию
  (RD03/RD23, смотреть на наклейке снизу роутера) со списком поддержки openwrt.org, не все
  ревизии поддерживаются. План обкатки: сначала OpenWrt + Tailscale (проверить что камера
  стабильно доступна из облака), потом добавить Podkop поверх и проверить не деградирует ли
  поток камеры при параллельной нагрузке (кто-то смотрит YouTube). Скоуп по-прежнему фазами:
  **Phase 1 (код готов, см. выше) — только лайв без записи**; Phase 2 — 3-дневный
  retention/перемотка/скорость после того, как связка роутера обкатана.
  **2026-07-23**: роутер на OpenWrt подтверждён (уже стоит дома), но не подтверждено, реально
  ли на нём поднят и работает Tailscale — Александр проверит по возвращении домой (`tailscale
  status` по SSH). Также нужно выяснить, отдаёт ли камера HTTP/MJPEG напрямую (без ffmpeg) —
  сильно упрощает мост, если да, раз перекодировать RTSP→HLS на слабом роутере рискованно.
- **Публичная self-serve регистрация компаний** — реализована 2026-07-18, см. "Мультитенантность" →
  подраздел "Публичная self-serve регистрация" выше. Осталось (не блокирует): капча, телефон-верификация,
  логика триала/оплаты — актуально когда дойдёт очередь до рекламы/публичного запуска.

## Design System
Читай `DESIGN.md` перед любыми визуальными/UI-решениями во фронте. Шрифты, цвета, spacing,
motion — определены там. **Статус на 2026-07-17: внедрено частично** — Sidebar/Layout/Дашборд
на новой тёмной системе ("Refined Industrial", золото+Fraunces), остальные 8 страниц пока на
старой светлой (cream/ink/terracotta) — сознательное поэтапное решение, не забытый долг. Не
переопределяй `--color-terracotta`/`--color-cream`/`--color-ink` — 140 использований по всем
страницам, точечное изменение значения токена молча перекрасит всё приложение. Новые
premium-токены — аддитивные (`--color-premium-*` в `index.css`), не трогают старые.

## Обновление фронта после деплоя (добавлено 2026-07-18)
Вместо разлогина всех пользователей после каждого прод-деплоя (обсуждали, но это грубо —
задело бы и обычные рестарты, не только осознанные изменения) — баннер снизу «Доступно
обновление интерфейса», применяется строго по клику на кнопку. `GET /api/version`
(`app/config.py::APP_VERSION`, публичный, без авторизации) отдаёт `RENDER_GIT_COMMIT` —
Render сам прописывает эту переменную при деплое, ничего не надо менять в Dockerfile.
`UpdateBanner.tsx` (смонтирован в `App.tsx` вне `<Routes>`, виден на любой странице
включая `/login`) поллит раз в минуту, сравнивает с версией при первом успешном ответе.
Сессию/токен не трогает вообще — только подгружает новый JS/CSS через reload.

## Роадмап реализации
1. ✅ Postgres + SQLAlchemy-модели + seed-запись Founder (заменил исходный пункт "сервисный аккаунт Google + структура обоих файлов")
2. ✅ Каркас логина (bcrypt, JWT, разграничение ролей через `require_roles`)
3. ✅ Раздел "Сырьё" (в UI называется «Компоненты» — таблицы Materials/Transactions не переименованы): инвентаризация, приход/расход
4. ✅ Раздел "Производство": смены, списание сырья по рецепту, брак → данные для КПД
5. ✅ Раздел "Продажи" (в UI называется «Отгрузка» — таблица Sale не переименована): списывает кол-во из «готово к отгрузке» у Product + управление рецептами/продуктами
6. ✅ Управление сотрудниками (`app/routers/users.py`, `StaffPage.tsx`): создание, увольнение, сброс пароля — Founder/Developer
   ✅ Управление компаниями (2026-07-18): `app/routers/companies.py`, `CompaniesPage.tsx` — только
   Developer, пункт «Компании» в меню профиля рядом с «Сотрудники»/«Техпанель». Заводит компанию +
   первого Developer-аккаунта, Founder заводится отдельно (`create_founder.py --company-id`).
   ✅ Мульти-компанийные пользователи (2026-07-18, тот же день) — CompanyMembership вместо
   User.company_id/role, вход с выбором компании при нескольких членствах, переключалка в
   сайдбаре, существующий логин при создании = приглашение, не ошибка. См. отдельный
   раздел "Мульти-компанийные пользователи" выше.
7. ✅ Дашборд: `dashboard.py` отдаёт остатки ниже минимума, топ сырья по расходу и последние 10 движений;
   `GET /sales/top` — топ-3 продукта по отгрузке; `GET /dashboard/spend` — траты (сумма прихода
   сырья qty×price, только `type=приход` с заполненной ценой — расход/корректировка остатков это не
   деньги) помесячно + топ-5 материалов по тратам, с опциональными `date_from`/`date_to` (без
   параметров — вся история); `GET /dashboard/kpi` — выработка (партии×выход_партии − брак) помесячно
   на сотрудника, только по Производству (Упаковка — отдельный будущий блок, см. "Отложенные темы").
   Единого сводного КПД-балла нет — Founder осознанно решила пока считать только выработку по
   количеству, без взвешивания скорости/брака.
   ✅ Виджет-конструктор (2026-07-16): `app/dashboard_widgets.py` — реестр из 11 виджетов (ключ,
   заголовок, тип отрисовки list/bar/line/donut/stat, размер сетки по умолчанию/минимальный,
   функция расчёта данных). Раскладка — `DashboardWidgetLayout` (company_id, user_id, widget_key,
   x, y, w, h) — **per-user с 2026-07-20** (решение Александра, до этого была общая на всю
   мастерскую — founder/developer видели одно и то же, так изначально решила Founder; миграция
   `c7d8e9f0a1b2` сбросила старые общие раскладки, каждый настраивает заново). Изоляция теперь
   `(company_id, user_id, widget_key)`, не только `company_id`.
   Эндпоинты: `GET /dashboard/widgets/catalog`, `GET/PUT /dashboard/widgets/layout`,
   `GET /dashboard/widgets/{key}/data`. Фронт — `DashboardPage.tsx` на `react-grid-layout` (drag/resize
   мышью в стиле iOS-виджетов, `/legacy` — v1 flat-props API, вторая версия либы 2026 переписана на
   хуки и композитные пропсы) + `recharts` для графиков (палитра — см. skill `dataviz`). Известный
   Vite-гоча: react-draggable (транзитивная зависимость) читает `process.env.NODE_ENV` в браузере —
   без `define` в `vite.config.ts` падает `ReferenceError` при первом mousedown на драге.
8. ✅ Техпанель для разработчика: `app/routers/techpanel.py` (только developer) — статус API
   (пинг БД, аптайм), последние 200 записей логгера в памяти (`app/techlog.py`, не персистентно),
   сброс `lru_cache` секретов (`app/config.py::_secrets`). Фронт: `TechPanelPage.tsx`, ссылка в
   меню профиля рядом с «Сотрудники».
9. ⬇️ Telegram-бот — низкий приоритет (2026-07-17, решение Founder): раз веб-версия доступна
   с телефона, набор команд (/остатки /траты_месяц /прогноз /топ_сырьё /топ_продажи) сам по
   себе не даёт ничего сверх браузера. Единственный сценарий, ради которого бот всё же имеет
   смысл — push-уведомления (низкий остаток, новая продажа и т.п.), которые веб сам не
   пришлёт: этого пока тоже не нужно, но может понадобиться позже. Не в активной разработке.
10. 🔶 Видеонаблюдение — вкладка-заглушка заведена, техрешение не финализировано (см. "Отложенные темы")

## Как со мной общаться (предпочтения пользователя)
- Обращение на "ты", тон дружелюбный, профессиональный, без воды
- Код сразу готовый к копированию, с лаконичными комментариями
- Если задача сложная — сначала коротко архитектурное решение, потом реализация
- **Работа идёт через Telegram-мост (2026-07-21, жёсткое правило, повторялось 6+ раз, каждый
  раз с потерей времени Александра).** Простой текстовый вывод ассистента НЕ доходит до
  Telegram-чата — доходит только явный вызов инструмента `mcp__plugin_telegram_telegram__reply`
  (или `edit_message`). **На КАЖДОЕ сообщение от Александра — первым делом, до какой-либо
  работы — отправь короткое подтверждение через `reply`**: "принял, делаю X" ИЛИ, если есть
  неясности, сразу задай уточняющие вопросы этим же `reply`-сообщением (никогда не
  `AskUserQuestion` в этом контексте — не долетает). Финальный результат — тоже отдельным
  `reply`. Правило не опционально и не зависит от того, длинная задача или короткая: пропуск
  этого шага для Александра неотличим от "сообщение не дошло", он теряет время, ожидая ответ,
  которого не будет. Если в рамках одного ответа не было ни одного вызова `reply` — ответ не
  считается завершённым.

## Testing

**Статус:** `backend/tests/` — pytest-набор поверх FastAPI `TestClient` + SQLite in-memory
(`tests/conftest.py`: фикстуры `db_session`/`client`, `get_db` подменяется на сессию к SQLite,
токены минтятся напрямую через `create_access_token`, без прогона bcrypt-логина в каждом тесте
кроме `test_auth.py`; `make_user`/`make_company`/`default_company_id` — все ORM-объекты, созданные
напрямую в тесте, а не через API, обязаны получить `company_id`, иначе NOT NULL упадёт). Реальный
Postgres не трогается. `pytest`/`httpx` — в `backend/requirements-dev.txt`
(не в основном `requirements.txt`, чтобы не тянуть dev-зависимости на прод), установлены в `backend/.venv`.
Покрыто: auth (логин/уволенный/лог входов/ролевой доступ), ingredients (баланс приход/расход/корректировка),
production (проверка достаточности сырья — см. ниже, worker видит только свои записи), sales (проверка
«готово к отгрузке» — см. ниже), recipes (пустой состав → 422, ролевой доступ), packaging, counterparties
(валидация телефон/ИНН/КПП/ОГРН), users (создание/дубликат логина/увольнение отзывает доступ сразу),
dashboard (spend: только приходные транзакции с ценой, фильтр по датам; kpi: группировка по
месяцу+сотруднику; ролевой доступ), **test_multitenancy.py** — изоляция между компаниями по каждому
типу ресурса (материалы/рецепты/продукты/продажи/контрагенты/сотрудники/лог входов/раскладка
дашборда), кросс-компанийный доступ по id всегда 404. **test_companies.py** — только Developer,
вариант А (существующий логин = привязка, не ошибка). **test_multi_company_login.py** — вход с
несколькими компаниями (выбор/finalize/переключение), отказ чужого/не-pending токена.
**test_register.py** — публичная саморегистрация: успех, вариант А (существующий логин, неверный
пароль, уволенный аккаунт), одинаковое сообщение об ошибке для "неверный пароль"/"уволен" (не оракул
для перебора логинов), rate-limit по IP и по `X-Forwarded-For` отдельно, превышение длины полей/пароля
(422, не 500), изоляция данных нового тенанта. **test_auth.py** также покрывает ограничение входа по
сети мастерской: разрешено/отклонено по IP, founder не ограничен, выключено по умолчанию, fail-closed
при нерезолвящемся DDNS-хосте (тесты используют `localhost`→`127.0.0.1` и зарезервированный `.invalid`
TLD вместо реального DNS — детерминированно, без сетевой флакиности). **test_surveillance.py** —
настройки потока/скриншоты, ролевой доступ, изоляция между компаниями.
Запуск: `cd backend && .venv/bin/python -m pytest tests/ -q`.

Правила на будущее:
- Новая функция с логикой → тест на неё.
- Починка бага → регрессионный тест.
- Обработка ошибки → тест, который эту ошибку триггерит.
- Условная ветка (if/else) → тест на оба пути.
- Не коммитить код, который ломает существующие тесты.
- **/code-review и /security-review перед КАЖДЫМ пушем на прод (2026-07-18, правило Founder)** —
  не разово по запросу. Первый прогон на дифе редизайна поймал реально утёкший в CLAUDE.md пароль
  камеры и несколько живых регрессий (мобильный хедер, WCAG-контраст, невидимый grain) — правило
  подтверждено ценностью находок, применять на каждый `git push origin main`, включая мелкие
  однофайловые правки.
